import Foundation

/// Marker protocol/macro scaffolding for future @APIClient support.
/// This will eventually generate network calling code in the frontend
/// that matches Hummingbird routes in the backend.
public struct APIClientMarker {
    // Currently empty — structural placeholder for Phase H/I
    public init() {}
}
